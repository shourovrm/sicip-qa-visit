<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- for the "informed you" trip notice. runtime request prompt is wired up by the shell task. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- pre-scoped-storage Downloads write for the TA/DA bill PDF (api 29+ uses MediaStore, no permission needed) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:label="SICIP QA Visit"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- shares the generated TA/DA bill PDF (cache dir) to the share sheet, see pdf/BillPrinter.kt -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="bd.sicip.qavisit.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
